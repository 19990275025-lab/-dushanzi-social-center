# 更新日志

本文件从项目文档规范化 V1.0 开始记录新媒体运营中心的功能变化。版本号采用 `social-vX.Y.Z`；在创建正式 Git tag 前，变化保留在 `[Unreleased]`。

格式参考 Keep a Changelog，分类固定为：新增、优化、修复、数据库调整、接口调整。

## [Unreleased]

### 新增

- 建立系统架构、模块状态、数据库、API、数据流、版本路线和部署文档。
- 增加 README 文档导航、架构简图、开发状态和当前版本说明。
- 建立根目录 `CHANGELOG.md`，从当前阶段持续记录版本变化。

### 优化

- 明确抖音当前主链、快手/微博数据准备状态和视频号远期边界。
- 明确 WorkBuddy 已接入，MediaCrawler 与 Agent-Reach 仅完成评估/规划。
- 明确当前 AI 以规则模型为主，避免把预留能力描述为正式大模型能力。

### 修复

- 文档层面标注粉丝分析响应中存在但尚无实际 Route Handler 的预留采集路径。
- 文档层面区分热点标准主表 `hot_topics` 与兼容表 `HOT_TOPIC_DATA`。

### 数据库调整

- 无。本次仅整理现有 schema，没有执行迁移或修改数据结构。

### 接口调整

- 无。本次仅记录现有接口，没有改变路径、请求或响应。

## 版本链接

当前尚未创建正式 `social-v*` tag。首个建议基线为 `social-v0.9.0`，需在功能验收、测试和发布检查完成后创建。
